"use strict";
// src/modules/people-finder.ts
// Option B: AI-suggested contacts — no scraping, no LinkedIn risk
Object.defineProperty(exports, "__esModule", { value: true });
exports.findContacts = findContacts;
const helpers_1 = require("../utils/helpers");
async function findContacts(company) {
    // Contacts are now generated by Gemini in ai-processor.ts
    // This module is kept for future use
    (0, helpers_1.log)('people', `Contacts for ${company} handled by AI processor`);
    return [];
}
